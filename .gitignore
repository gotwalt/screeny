target/
backup/*.bin
captures/
.DS_Store
lab/out/
lab/Cargo.lock
lab/xtensa-bench/target/
lab/xtensa-bench/Cargo.lock

# The Studio's state directory for a local run (card 106). In Docker this is a
# volume at /data instead; see SCREENY_STATE_DIR.
.screeny-studio/

# WiFi credentials never go in git (firmware/build.rs reads them at build time)
wifi.env

# docker compose reads `.env` from the project directory. That file is host
# state - ports, the render gid, a timezone - and never belongs in the repo.
.env

# The development process record (task board, coordinator playbook, agent
# instructions) is kept locally and is not part of the published tree.
.claude/
CLAUDE.local.md
