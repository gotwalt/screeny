[workspace]
resolver = "2"
members = ["crates/*"]
# firmware/ uses the esp toolchain and an Xtensa target; lab/ is card 002's frozen
# codec reference. Neither belongs in the host workspace.
# art/ is the generative art system: its own workspace (Tauri + wgpu), merged from
# its own branch. It depends on crates/screeny by path; it is not a member here.
exclude = ["firmware", "lab", "lab/xtensa-bench", "lab/nostd-check", "art"]

[workspace.package]
edition = "2021"
license = "MIT"

[profile.release]
debug = 1
