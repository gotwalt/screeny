{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The studio window may call the app's own commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
