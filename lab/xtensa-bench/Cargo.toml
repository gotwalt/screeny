[package]
name = "xtensa-bench"
version = "0.1.0"
edition = "2021"
description = "Counts Xtensa instructions executed per frame decode, under qemu-system-xtensa."

[dependencies]

[profile.release]
opt-level = 3
lto = true
panic = "abort"
debug = false

[workspace]
